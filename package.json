{
  "name": "space-rocket-builder",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:assets": "node scripts/build-models.mjs && node scripts/build-sounds.mjs",
    "build": "npm run build:assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.167.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "playwright-core": "^1.61.1",
    "vite": "^8.1.0"
  }
}
